import styled from 'styled-components'

export const StyledLoginBasket = styled.div`
    display: flex;
    align-items: center;

    .basket-icon {
        cursor: pointer;
        color: #888;

        &:hover {
            color: #1789dc;
            }
    }
`