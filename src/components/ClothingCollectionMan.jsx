import React from "react"

export const ClothingCollectionMan = (props) => {
  return (
    <div className="collection-title container">
      <span className="collection-title__text">{props.title}</span>
    </div>
  )
}